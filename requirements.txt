# ASTRA core runtime requirements (package will not import without these)
numpy>=1.24
scipy>=1.10
scikit-learn>=1.2
networkx>=3.0
sympy>=1.12
pandas>=2.0
psutil>=5.9

# Optional extras -- install with `pip install -e .[all]` or individually:
#   astropy>=5.3        FITS I/O, archive access
#   torch>=2.0          deep-learning models (astro_physics.deep_learning)
#   reportlab>=4.0      PDF generation
#   pillow>=10.0        image handling for PDF generation
#   matplotlib>=3.7     plotting
#   emcee>=3.1          MCMC sampling
#   corner>=2.2         posterior corner plots
#   statsmodels>=0.14   time-series statistics
#   scikit-image>=0.22  skeletonisation backend for FilamentFinder
